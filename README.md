# Content
```
├── array/
│   ├── difference_array.md
│   │   Add a value to a range [L, R] in O(1) time. Process all updates and
│   │   get the final array in O(n) time.
│   └── kadanes_algo.md
│       Find the maximum sum of any contiguous subarray O(1) space O(n) time.
│
├── dp/
│   ├── levenshtein_distance.md
│   │   Find minimum edits (insert/delete/substitute) to transform one string
│   │   into another using dynamic programming in O(mn) time.
│
│
├── misc/
│   ├── stl.md
│   │   priority_queue, hash combine, randomization
│   ├── math.md
│   │   lines intersaction, point on line
```
